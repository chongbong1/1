# Исправления в FMM для логарифмического потенциала

## Критические ошибки найдены и исправлены

### Проблема 1: Неправильный знак в L₀ для монополя

**Было (НЕПРАВИЛЬНО):**
```fortran
sys%cells(ti,tj)%L(0) = sys%cells(ti,tj)%L(0) + &
                       sys%cells(si,sj)%M(0) * log(abs(z0_scaled))
```

**Стало (ПРАВИЛЬНО):**
```fortran
sys%cells(ti,tj)%L(0) = sys%cells(ti,tj)%L(0) - &
                       sys%cells(si,sj)%M(0) * log(abs(z0_scaled))
```

**Объяснение:** Для логарифмического потенциала φ = -log|r|, монопольный вклад в локальное разложение должен иметь отрицательный знак.

---

### Проблема 2: Неправильная формула для вклада M_k в L₀

**Было (НЕПРАВИЛЬНО):**
```fortran
do k = 1, sys%p_max
  sys%cells(ti,tj)%L(0) = sys%cells(ti,tj)%L(0) + sys%cells(si,sj)%M(k)
end do
```

**Стало (ПРАВИЛЬНО):**
```fortran
z0_pow = z0_scaled
do k = 1, sys%p_max
  sys%cells(ti,tj)%L(0) = sys%cells(ti,tj)%L(0) + &
                         sys%cells(si,sj)%M(k) * z0_pow
  z0_pow = z0_pow * z0_scaled
end do
```

**Объяснение:** Высшие мультипольные моменты вносят вклад в L₀ через степени вектора смещения z₀^k, а не просто суммируются.

---

### Проблема 3: Неправильные степени z₀ в формуле для L_j

**Было (НЕПРАВИЛЬНО):**
```fortran
z0_pow = z0_inv_scaled  ! (1/z0)^1
do j = 1, sys%p_max
  temp_sum = -sys%cells(si,sj)%M(0) / real(j, dp)

  do k = 1, sys%p_max
    binom_coef = sys%binomial(j+k-1, k-1)
    temp_sum = temp_sum + sys%cells(si,sj)%M(k) * binom_coef
  end do

  sys%cells(ti,tj)%L(j) = sys%cells(ti,tj)%L(j) + temp_sum * z0_pow
  z0_pow = z0_pow * z0_inv_scaled
end do
```

**Стало (ПРАВИЛЬНО):**
```fortran
z0_inv_pow_j = z0_inv_scaled  ! (1/z0)^1 for j=1
do j = 1, sys%p_max
  ! Monopole contribution: -M_0 / (j * z0^j)
  temp_sum = -sys%cells(si,sj)%M(0) * z0_inv_pow_j / real(j, dp)

  ! Higher moment contributions: Σ M_k · C(j+k-1,k-1) / z0^(j+k)
  z0_inv_pow_jk = z0_inv_pow_j * z0_inv_scaled  ! Start with (1/z0)^(j+1)
  do k = 1, sys%p_max
    if (j + k - 1 <= ubound(sys%binomial, 1) .and. &
        k - 1 <= ubound(sys%binomial, 2)) then
      binom_coef = sys%binomial(j+k-1, k-1)
      temp_sum = temp_sum + sys%cells(si,sj)%M(k) * binom_coef * z0_inv_pow_jk
    end if
    z0_inv_pow_jk = z0_inv_pow_jk * z0_inv_scaled  ! (1/z0)^(j+k+1)
  end do

  sys%cells(ti,tj)%L(j) = sys%cells(ti,tj)%L(j) + temp_sum
  z0_inv_pow_j = z0_inv_pow_j * z0_inv_scaled
end do
```

**Объяснение:** Правильная формула M2L требует степени (j+k) в знаменателе для вклада M_k в L_j. Это критично для точности!

---

## Математическая основа исправлений

### Разложение логарифмического потенциала

Для 2D логарифмического ядра:
```
φ(z) = -log|z - z₀|
```

Используя комплексное разложение:
```
-log|z - z₀| = -log|z| - Re[Σ_{k=1}^∞ (z₀/z)^k / k]  при |z₀| < |z|
```

### M2L формулы

При переносе мультипольного разложения из источника в локальное разложение в цели:

**Вектор смещения:** z₀ = z_target - z_source

**Локальное разложение:**

```
L₀ = M₀ · (-log|z₀|) + Σ_{k=1}^p M_k · z₀^k

L_j = -M₀/(j·z₀^j) + Σ_{k=1}^p M_k · C(j+k-1, k-1) / z₀^(j+k)    (j ≥ 1)
```

где C(n,k) - биномиальные коэффициенты.

---

## Источники

Правильные формулы взяты из:

1. **Greengard & Rokhlin (1987)** "A Fast Algorithm for Particle Simulations"
   - Оригинальная статья FMM
   - [PDF](http://www.stat.uchicago.edu/~lekheng/courses/302/classics/greengard-rokhlin.pdf)

2. **fmm2d library (Flatiron Institute)**
   - Современная эталонная реализация
   - [GitHub](https://github.com/flatironinstitute/fmm2d)
   - [Документация](https://fmm2d.readthedocs.io/)

3. **Beatson & Greengard** "A short course on fast multipole methods"
   - Учебный курс с детальными выводами
   - [NYU Materials](https://math.nyu.edu/~greengar/shortcourse_fmm.pdf)

---

## Ожидаемое улучшение точности

После исправлений:

| p_order | Ожидаемая точность (относительная ошибка силы) |
|---------|-----------------------------------------------|
| 10      | ~10⁻⁵ - 10⁻⁶                                  |
| 20      | ~10⁻⁸ - 10⁻¹⁰                                 |
| 30      | ~10⁻¹² - 10⁻¹⁴ (машинная точность)           |

**До исправлений:** ошибки были значительно больше из-за неправильных формул M2L.

**После исправлений:** точность должна экспоненциально улучшаться с ростом p_order.

---

## Рекомендации для дальнейшего использования

### 1. Выбор параметров для высокой точности

Для достижения машинной точности (~10⁻¹²):
```fortran
p_order = 30    ! Порядок разложения
n_cells = 10    ! 10×10 сетка для 100-1000 частиц
```

### 2. Компиляция

```bash
make          # Продакшн версия с оптимизацией
make debug    # Отладочная версия с проверками
```

### 3. Запуск

```bash
./vortex_fmm
```

### 4. Проверка точности

Программа автоматически сравнивает FMM с прямым расчетом и выводит:
- Максимальную относительную ошибку
- Среднюю относительную ошибку
- Время расчета FMM vs прямой метод

### 5. Дальнейшие улучшения

Если нужна еще более высокая скорость:
- Реализовать иерархическое дерево (octree) вместо равномерной сетки
- Добавить M2M и L2L операции для многоуровневого FMM
- Распараллелить с OpenMP

Но для равномерной сетки текущая реализация оптимальна!

---

## Тестирование

Программа включает автоматическое тестирование:

1. **Сравнение с прямым расчетом** - верификация точности
2. **Первые 5 частиц** - детальный вывод сил для проверки
3. **Статистика ошибок** - максимальная и средняя относительная ошибка
4. **Динамика вихрей** - симуляция с выводом в CSV

---

## Контакты и поддержка

При обнаружении проблем проверьте:
1. Правильность компиляции (gfortran >= 4.8)
2. Размер системы (для > 10000 частиц нужна иерархия)
3. Граничные условия (текущая реализация: отражающие границы)

Удачи с моделированием сверхпроводников!
