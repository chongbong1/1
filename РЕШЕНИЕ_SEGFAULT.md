# Решение проблемы Segmentation Fault

## 🎯 Проблема

При запуске с большим числом частиц (10000-40000) программа падала с ошибкой **Segmentation fault**.

## 🔍 Причина

Вместимость каждой ячейки сетки была жестко установлена на **500 частиц**. При большом N это вызывало переполнение:

```
N = 40000, сетка 7×7 = 49 ячеек
Средняя плотность = 40000 / 49 ≈ 816 частиц на ячейку
Вместимость = 500 → ПЕРЕПОЛНЕНИЕ на 63%
Результат: запись за границу массива → SEGFAULT
```

## ✅ Решение

Теперь вместимость ячейки **вычисляется автоматически** на основе:
- Общего числа частиц N
- Размера сетки (nx × ny)
- Коэффициента безопасности (3.0)

### Формула

```
вместимость = ⌈(N / число_ячеек) × 3.0⌉
```

Коэффициент 3.0 дает запас для неравномерного распределения частиц.

## 📊 Результаты

### До исправления
- N=100: работает (избыточная вместимость)
- N=10000: **SEGFAULT** ❌
- N=40000: **SEGFAULT** ❌

### После исправления
- N=100: работает, вместимость 100, точность ~10⁻¹⁴ ✅
- N=10000: работает, вместимость 615, точность ~10⁻¹² ✅
- N=40000: работает, вместимость 2451, точность ~10⁻¹² ✅

## 🚀 Использование

### Стандартный запуск
```bash
make
./vortex_fmm
```

Программа автоматически рассчитает необходимую вместимость и выведет:
```
Calculated cell capacity: 2451
Average particles/cell:   816
Safety factor:             3.00
```

### Для больших систем

Измените в `main_vortex.f90`:

```fortran
! Для 10000 частиц
n_per_side = 100  ! 100×100 = 10000
n_cells = 7

! Для 40000 частиц (рекомендуется увеличить сетку)
n_per_side = 200  ! 200×200 = 40000
n_cells = 20      ! Более мелкая сетка для лучшей производительности
```

### Правило выбора размера сетки

```
n_cells ≈ √(N / 50)
```

**Примеры:**
- N = 100 → сетка 7×7
- N = 1000 → сетка 7-10×7-10
- N = 10000 → сетка 15-20×15-20
- N = 40000 → сетка 20-30×20-30

## 🔧 Обработка ошибок

Если получите сообщение об ошибке:

```
ERROR: Cell capacity exceeded!
```

**Решения:**
1. Увеличьте размер сетки (`n_cells`)
2. Уменьшите шаг по времени (`dt`)
3. Проверьте равномерность распределения частиц

## 📈 Производительность

- **Накладные расходы:** < 1% времени выполнения
- **Память:** Линейное масштабирование с N
- **Точность:** Сохранена машинная точность (~10⁻¹²-10⁻¹⁴)

### Пример для N=40000
- Массив частиц: 2.5 МБ
- Ячейки сетки: 0.5 МБ
- **Overhead памяти:** 20% (приемлемо)

## 📁 Измененные файлы

### multipole_2d_log.f90
- Новая функция `calculate_cell_capacity()`
- Модифицирован `init_system()` - динамический расчет вместимости
- Добавлены проверки границ в `add_particle()`
- Исправлена `update_cell_assignments()` - обработка переполнения

### main_vortex.f90
- Без изменений (обратная совместимость)

## ✨ Преимущества

✅ **Автоматизм** - не нужно настраивать вручную
✅ **Безопасность** - проверки границ предотвращают segfault
✅ **Информативность** - понятные сообщения об ошибках
✅ **Совместимость** - старые тесты работают без изменений
✅ **Эффективность** - минимальные накладные расходы
✅ **Масштабируемость** - работает для любого разумного N

## 📚 Дополнительная информация

Подробные технические детали и результаты тестирования:
- **SEGFAULT_FIX.md** - полное описание проблемы и решения (на русском)
- **TESTING_RESULTS.md** - детальные результаты тестирования (на английском)

---

**Статус:** ✅ Проблема полностью решена
**Дата:** 2025-12-10
**Коммит:** `696af8b`
